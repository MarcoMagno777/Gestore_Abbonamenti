FROM node:22-bookworm-slim

WORKDIR /app

COPY entrypoint-node.sh /entrypoint-node.sh
RUN chmod +x /entrypoint-node.sh

ENV NPM_CONFIG_CACHE=/tmp/.npm \
    CHOKIDAR_USEPOLLING=true

CMD ["sh", "/entrypoint-node.sh"]